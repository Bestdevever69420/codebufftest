package com.example.mymod.entity;

import net.minecraft.entity.item.EntityBoat;
import net.minecraft.world.World;

public class EntityPyroxeneBoat extends EntityBoat {
    public EntityPyroxeneBoat(World worldIn) {
        super(worldIn);
    }

    public EntityPyroxeneBoat(World worldIn, double x, double y, double z) {
        super(worldIn, x, y, z);
    }
}